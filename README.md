# khera
This is basically a python based project dictionary based on Python 3.6.it basically uses modules requests,json in python.This project is based on searching a meaning of a word in a give text file if not than it would access Oxford API to search the meaning.it is basically a project to search as many as words as infinite loop is running in it. 
